# Document Reader Android runtime

Place **`documentreadersdk.aar`** in this folder (same artifact as `DocumentReader-Android-App/libdocsdk/`).

Do not commit the AAR — download from the FacePlugin Drive Android folder.
